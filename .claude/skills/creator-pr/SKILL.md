---
name: creator-pr
description: Create GitHub pull requests from current changes in Korean or English. Use when the user asks to "create a PR", "PR 만들어줘", "open a PR", "make a pull request", or wants to submit work-in-progress to GitHub. Automatically analyzes git diffs, generates PR descriptions in the chosen language, creates feature branches, and opens PRs via GitHub CLI.
context: fork
---

# PR Creator

Automate the process of creating pull requests on GitHub. This skill takes your current git changes, generates a descriptive PR, creates a feature branch if needed, and opens it on GitHub using the `gh` CLI.

## What This Skill Does

1. **Analyzes changes**: Reads current `git diff` to understand what's changed
2. **Detects language**: Automatically detects user language (Korean or English) or accepts explicit choice
3. **Generates description**: Creates PR title and body based on commit messages and file changes
4. **Applies template**: Uses Korean (references/pr_template_ko.md) or English (references/pr_template_en.md) template
5. **Creates branch**: Generates a feature branch name (e.g., `feature/responsive-view`) and switches to it if needed
6. **Commits changes**: Stages and commits any uncommitted changes
7. **Opens PR**: Uses `gh pr create` to open the PR on GitHub
8. **Returns link**: Provides the PR URL for sharing

## When to Use

- User says "create a PR" or "make a pull request"
- User wants to submit changes to GitHub
- User has completed a feature and wants to open it for review
- Work-in-progress needs to be pushed as a draft PR

## Workflow

### Input: What the User Provides

The user should provide:
- **PR Title** (required): Short, descriptive title (e.g., "Add responsive view to component preview")
- **Language** (optional): `ko` (Korean) or `en` (English); auto-detected from user input if not specified
- **PR Description** (optional): Additional context or testing notes (in the same language)
- **Branch name** (optional): Custom branch name; auto-generated if not provided

**Example user prompts:**

English:
```
"Create a PR for the responsive view feature"
"Open a PR: title='Fix API timeout', language=en"
"PR with branch feature/auth-flow"
```

Korean:
```
"PR 만들어줘: 반응형 뷰 기능 추가"
"PR 생성: 제목='API 타임아웃 수정', language=ko"
"PR 생성, 설명='모바일/태블릿/데스크탑에서 테스트 완료'"
```

### Steps the Skill Performs

#### Step 1: Verify Git Status

```bash
git status
git diff --stat
```

Check if there are uncommitted changes. If none, inform the user there's nothing to commit.

#### Step 2: Generate Branch Name

If the user doesn't provide one, derive it from the PR title:
- Input: "Add responsive view to component preview"
- Output: `feature/add-responsive-view`

Rules:
- Prefix with `feature/` (or `fix/`, `docs/` based on commit message type)
- Lowercase, replace spaces with hyphens
- Max 50 characters total

#### Step 3: Create and Switch Branch

```bash
git checkout -b <branch-name>
```

If branch already exists, checkout without creating.

#### Step 4: Stage and Commit Changes

```bash
git add .
git commit -m "<conventional commit message>"
```

Generate commit message:
- Analyze the PR title to infer the type (feat/fix/docs/style/refactor/chore)
- Format: `<type>: <message>`
- Example: `feat: add responsive view buttons to preview header`

#### Step 5: Generate PR Description

Select template based on detected or specified language:
- **Korean**: Use `references/pr_template_ko.md` (Korean template)
- **English**: Use `references/pr_template_en.md` (English template)

**Korean template structure:**
```markdown
## 요약
[1-2문장 요약]

## 변경사항
- [주요 변경 사항]

## 테스트 방법
[변경사항을 검증하는 방법]

---
creator-pr 스킬로 생성됨
```

**English template structure:**
```markdown
## Summary
[1-2 sentence summary]

## Changes
- [Key changes]

## Test plan
[How to verify the changes work]

---
Generated by creator-pr skill
```

Populate based on:
- Git commit messages
- File names changed (e.g., "Modified src/components/LivePreview.tsx")
- Diff content (identify what was added/removed)

#### Step 6: Push and Create PR

```bash
git push origin <branch-name>
gh pr create --title "<title>" --body "<body>" --draft
```

Use `--draft` flag to create a draft PR (user can mark ready).

Return the PR URL.

## Error Handling

- **No changes**: "No uncommitted changes found. Create some changes first, then run this skill again."
- **Detached HEAD**: "Cannot create PR from detached HEAD. Switch to a branch first."
- **No GitHub credentials**: "GitHub CLI is not authenticated. Run `gh auth login` and try again."
- **Branch conflicts**: "Branch already exists. Please resolve conflicts or use a different branch name."

## Configuration

This skill uses:
- **git**: Version control (built-in on most systems)
- **gh**: GitHub CLI (install via `brew install gh` or https://cli.github.com)

User must be authenticated: `gh auth login`

## Example Runs

### Example 1: English Feature PR

**User input:**
```
"Create a PR: Add dark mode toggle"
```

**Skill output:**
```
✓ Language detected: English
✓ Template applied: pr_template_en.md
✓ Branch created: feature/add-dark-mode-toggle
✓ Changes committed (3 files)
✓ PR opened: https://github.com/user/repo/pull/42
```

### Example 2: Korean Bug Fix

**User input:**
```
"PR 만들어줘: 컴포넌트 캐시 메모리 누수 수정"
```

**Skill output:**
```
✓ 언어 감지됨: 한국어
✓ 템플릿 적용: pr_template_ko.md
✓ Branch 생성: fix/component-cache-memory-leak
✓ 변경사항 커밋 (1 file)
✓ PR 생성: https://github.com/user/repo/pull/43
```

### Example 3: Explicit Language Specification

**User input:**
```
"Open a PR, title='Fix timeout issue', language=ko"
```

**Skill output:**
```
✓ Language specified: Korean
✓ Template applied: pr_template_ko.md
✓ Branch created: fix/timeout-issue
✓ Changes committed (2 files)
✓ PR opened: https://github.com/user/repo/pull/44
```

---

## Language Detection

The skill automatically detects language based on user input:
- If user input contains Korean characters (한글), default to Korean template
- If user input contains English text, default to English template
- If user explicitly specifies `language=ko` or `language=en`, use that template

## Template Management

All templates are managed in the `references/` directory:
- `references/pr_template_ko.md` — Korean PR template with guide
- `references/pr_template_en.md` — English PR template with guide

To customize templates, edit these files directly. Changes apply immediately to all future PRs.
